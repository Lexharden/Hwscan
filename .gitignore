# ── Compiled binaries ──
bin/
dist/
*.exe

# ── Alpine ISO builder (artefactos generados; los scripts sí van al repo) ──
build/alpine/base/
build/alpine/work/
build/alpine/output/

# Cualquier imagen ISO en el proyecto (base Alpine, hwscan-live, etc.)
*.iso
**/*.iso

# ── Exportaciones locales de HWSCAN ──
hwscan-*.json

# ── Go artifacts ──
*.o
*.a
*.so
*.dylib
*.test
*.out
coverage.txt

# ── Dependency directories ──
vendor/

# ── Go workspace ──
go.work
go.work.sum

# ── IDE / Editor ──
.idea/
.vscode/
*.swp
*.swo
*~
.DS_Store
Thumbs.db
.directory

# ── Temporary / Logs ──
tmp/
temp/
*.tmp
*.log

# ── Fyne cache (legacy) ──
.fyne/
